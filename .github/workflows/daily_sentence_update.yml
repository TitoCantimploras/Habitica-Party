name: 每日句子更新

on:
  schedule:
    - cron: '0 9 * * *'  # 每天 UTC 时间 9 点触发
  workflow_dispatch: # 允许手动触发

jobs:
  update_daily_sentence:
    runs-on: ubuntu-latest

    steps:
      - name: 获取每日句子
        id: get_sentence
        run: |
          # 从 API 获取每日句子
          response=$(curl -s "https://sentence.iciba.com/?c=dailysentence&m=getTodaySentence")
          echo "响应: $response"

          # 将内容和翻译存储到环境变量中
          echo "content=$content" >> $GITHUB_ENV
          echo "translation=$translation" >> $GITHUB_ENV

      - name: 更新 Habitica 队伍描述
        run: |
          # 发送 PUT 请求更新 Habitica 小组的描述
          response=$(curl -s -X PUT "https://habitica.com/api/v3/groups/party" \
          -H "x-api-user: ${{ secrets.HABITICA_USER_ID }}" \
          -H "x-api-key: ${{ secrets.HABITICA_API_KEY }}" \
          -H "Content-Type: application/json" \
          -d "{\"description\": \"一言 · A sentence of the day 🌹\\n\\n${{ env.content }}\\n\\n${{ env.translation }}\\n\\nWant to learn more about the party's purpose, rules, and other information? (Click here!)[https://github.com/Delta-Water/Habitica-Party/blob/main/README.md]\"}")

          # 检查更新是否成功
          if [[ $(echo "$response" | jq -r '.success') != true ]]; then
            echo "更新 Habitica 描述失败: $(echo "$response" | jq -r '.message')"
            exit 1
          fi

          echo "Habitica 描述更新成功"